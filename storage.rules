rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Rules for file access will go here.
    // For now, we'll keep it locked down.
    // match /{allPaths=**} {
    //   allow read, write: if request.auth != null;
    // }
  }
}
